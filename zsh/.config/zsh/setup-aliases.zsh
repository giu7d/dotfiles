#!/usr/bin/env zsh

# Basic aliases
alias c='clear'
alias x='exit'
alias cp='cp -rv' 
alias rm='rm -rIv'
alias mv='mv -v'
alias mkdir='mkdir -p'

# Update aliases
alias update='yay'  # Check system and AUR updates
alias updatef='flatpak update'