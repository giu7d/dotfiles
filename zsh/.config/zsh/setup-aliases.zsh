#!/usr/bin/env zsh

# Basic aliases
alias c='clear'
alias x='exit'
alias cp='cp -rv' 
alias rm='rm -rIv'
alias mv='mv -v'
alias mkdir='mkdir -p'

# Vim aliases
alias vim='nvim'
alias edit='nvim'

# Update aliases
alias update='yay -Syu'  # Check system and AUR updates
alias updatef='flatpak update'