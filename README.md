# Dotfiles

This repository contain the dotfiles for some of my systems.
The dotfiles are separated in branches:

1. `macos`
2. `linux-arch-gnome`
3. `__deprecated__linux-arch-hypr`

Access each branch to install its dependencies
